<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="by.grodno.zagart.observer.observerandroid">

    <application
        android:allowBackup="true"
        android:label="@string/string_sp"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ZeroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".activities.A1"/>
        <activity android:name=".activities.A2"/>
        <activity android:name=".activities.A3"/>
        <activity android:name=".SadActivity"/>
        <activity android:name=".HappyEndActivity"/>
    </application>

</manifest>